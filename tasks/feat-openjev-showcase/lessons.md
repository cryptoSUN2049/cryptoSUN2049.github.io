# Lessons

- “Put results here” refers to the page explicitly shown by the user. Confirm the target early rather than assuming another model-card update satisfies a personal-site request.
- The user accepted concrete ~26ms latency instead of an unsupported speedup claim. Preserve its model, hardware, timing boundary and separate-run provenance.
- Do not mix the new1,000-question base+adapter panel with the old80-question merged-model panel.

- Lead the personal-site entry with the user’s intended contribution: an open-source Jev reproduction series and its measured gains. Latency and architecture support this story rather than replacing it.
- Pages can serve new HTML with cached old CSS. Version the stylesheet URL when its rules change; verify computed styles on the deployed page.
